// Helper placeholder: prefer REST fetch in route handlers instead of SDK.
// This file intentionally avoids importing '@supabase/supabase-js' to keep the project
// lightweight and sidestep peer-dependency conflicts during install. If you later
// decide to use the SDK, you can reintroduce it here and adjust call sites accordingly.

export const UNUSED = true;
